<%= form_for(providers_payment_type) do |f| %>
  <% if providers_payment_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(providers_payment_type.errors.count, "error") %> prohibited this providers_payment_type from being saved:</h2>

      <ul>
      <% providers_payment_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %>
    <%= f.text_field :payment_type %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
